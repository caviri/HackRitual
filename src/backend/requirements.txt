# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Configuration
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client (GitHub export push, repo polling)
httpx==0.28.1

# Utils
python-multipart==0.0.12
python-dotenv==1.0.1
email-validator==2.2.0
pyyaml==6.0.3

# Image processing — server-side dither/halftone for uploads
pillow==10.4.0

# WASM scoring (MVP-3) — sandboxed scorer modules
wasmtime==25.0.0
